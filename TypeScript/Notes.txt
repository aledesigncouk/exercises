How to Run These Exercises:

    1. Save each exercise in a .ts file.

    2. Run with:

        $ tsc filename.ts --target es6 --strict
        $ node filename.js

    3. Fix any TypeScript errors until the code runs correctly.